# Ticket 2.9 — Remaining / Deferred Work

**Ticket**: 2.9 — Create / Edit / Revoke Verified Link
**Date**: 2026-08-10

## Deferred Items

### 1. Rate-limiting on the Verify Code API

- **What it is**: Rate-limiting attempt count on `POST /api/v1/verifier/verified-links/verify` to prevent brute-force verification code guessing.
- **Why it was deferred**: Explicitly marked out-of-scope for this ticket in `2.9-create-edit-revoke-verified-link.md` (lines 117–125).
- **What's needed to complete it later**:
  - Implement Redis/ARQ sliding-window rate-limiter middleware under `src/workers/` or `src/admin/rate_limit.py`.
  - Apply `@rate_limit(requests=10, window_seconds=60)` decorator or middleware to `POST /api/v1/verifier/verified-links/verify`.

### 2. Refactor `src/verifier/router.py` into a `routers/` package

- **What it is**: Splitting `src/verifier/router.py` (single flat file) into a `src/verifier/routers/` subpackage (matching `owner/routers/` and `issuer/routers/`).
- **Why it was deferred**: Flat file layout was maintained to avoid breaking existing verifier registration & invite imports across the codebase.
- **What's needed to complete it later**:
  - Move `router.py` into `src/verifier/routers/__init__.py`, `registration.py`, `invite.py`, and `verified_link.py`.
  - Update imports across the codebase.
